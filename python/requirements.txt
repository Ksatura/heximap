# HEXIMAP Python Port — Bağımlılıklar
# Kurulum: pip install -r requirements.txt

# ── Arayüz ────────────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-drawable-canvas>=0.9.3   # canvas köşe seçimi

# ── Görüntü işleme ────────────────────────────────────────────────────────
numpy>=1.26.0
Pillow>=10.0.0
opencv-python>=4.9.0               # ORB, stereoRectifyUncalibrated, remap
scipy>=1.12.0                      # Wiener filtresi
scikit-image>=0.22.0               # CLAHE (equalize_adapthist)

# ── Coğrafi veri ──────────────────────────────────────────────────────────
rasterio>=1.3.9                    # GeoTIFF okuma/yazma, önizleme
pyproj>=3.6.0                      # UTM projeksiyon dönüşümü

# ── Görselleştirme ────────────────────────────────────────────────────────
matplotlib>=3.8.0                  # DEM önizleme

# ── Sistem bağımlılıkları (pip ile kurulmaz) ──────────────────────────────
# zenity          → sudo apt install zenity      (dosya seçici, Linux/X11)
# libgl1          → sudo apt install libgl1      (OpenCV için)
